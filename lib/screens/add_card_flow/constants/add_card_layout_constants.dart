class AddCardLayoutConstants {
  // ✅ ADD THIS MISSING LINE
  // This defines the height of the card preview (credit card image)
  static const double previewCardHeight = 220.0;

  // Gap between preview card and section
  static const double previewToSectionGap = 40.0;

  // Distance from bottom of screen
  static const double sectionBottomInset = 16.0;

  // Section container
  static const double sectionRadius = 40.0;
  static const double sectionPadding = 16.0;
}